machine:
  environment:
    PATH: "$HOME/.asdf/bin:$HOME/.asdf/shims:$PATH"
    DB_URL: postgresql://ubuntu:@127.0.0.1:5432/api_test
    SECRET_KEY_BASE: "7uY6pDK6XstDPpgcnU+cL+dXE0e1cHrL21uDPiwOg84Co4rJOwzh9+EZhMocEYhb"
    MIX_ENV: test
dependencies:
  cache_directories:
    - ~/.asdf
    - deps
    - _build
  pre:
    - if ! asdf | grep version; then git clone https://github.com/HashNuke/asdf.git ~/.asdf; fi
    - if ! asdf plugin-list | grep erlang; then asdf plugin-add erlang https://github.com/HashNuke/asdf-erlang.git; fi
    - if ! asdf plugin-list | grep elixir; then asdf plugin-add elixir https://github.com/HashNuke/asdf-elixir.git; fi
    - erlang_version=$(awk '/erlang/ { print $2 }' .tool-versions) && asdf install erlang ${erlang_version}
    - elixir_version=$(awk '/elixir/ { print $2 }' .tool-versions) && asdf install elixir ${elixir_version}
    - yes | mix deps.get
    - yes | mix local.rebar
test:
  override:
    - mix coveralls.circle
