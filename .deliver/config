APP="api"

BUILD_HOST="api.makeorbreak.io"
BUILD_USER="deploy"
BUILD_AT="edeliver/build"

PRODUCTION_HOSTS="api.makeorbreak.io"
PRODUCTION_USER="deploy"

DELIVER_TO="edeliver"

AUTO_VERSION=git-revision+branch-unless-master
# pkoch was observing `git clean` not working if we had more than on path on this var.
GIT_CLEAN_PATHS="_build/prod/rel"
